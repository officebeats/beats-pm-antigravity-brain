---
description: Help me set up my PM brain. Walk me through the first-time setup.
---

1. Check if the brain is hydrated.
   // turbo
2. Run the command `./Setup-Brain.cmd` (Windows) or `./Setup-Brain.command` (Mac) to generate your private files.
3. Read `SETTINGS.md` to understand your configuration.
4. Read `START-HERE-FTUE.md` to guide the user through the next steps.
